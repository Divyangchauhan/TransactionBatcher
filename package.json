{
  "name": "transaction-batcher",
  "version": "1.0.0",
  "description": "Smart contracts for web3-batched-send",
  "license": "MIT",
  "scripts": {
    "start": "hardhat node",
    "build": "hardhat compile",
    "clean": "hardhat clean",
    "check": "hardhat check",
    "deploy": "hardhat run --network localhost scripts/deploy.ts",
    "deploy:mainnet": "hardhat run --network mainnet scripts/deploy.ts",
    "deploy:sepolia": "hardhat run --network sepolia scripts/deploy.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.4",
    "hardhat": "^2.20.1"
  }
}
